[package]
name = "evernote-cors-proxy"
version = "0.1.0"
edition = "2024"

[dependencies]
base64 = "0.22"
reqwest = { version = "0.12", default-features = false, features = ["blocking", "rustls-tls"] }
serde_json = "1"

[profile.release]
lto = true
strip = true
